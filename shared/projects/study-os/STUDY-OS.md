# STUDY-OS.md — StudyOS 项目速报

> **上位文件**：[CLAUDE.md](../../CLAUDE.md) §5 项目索引
> **仓库**：[yorhagengyue/study-os](https://github.com/yorhagengyue/study-os)（私有）
> **本地路径**：**本机未 clone**（2026-09-18 核实；Windows 那台在 `Desktop\canvas\study-os`，见 record 仓 DRAWER `c-9f1e3002`）
> **最后更新**：2026-09-18（首次登记。内容基于 main `a07b74f` + PR #2 分支的通读）

---

## 当前状态

**阶段**：基础版已交付并在真机跑通，等 owner 审 PR。仓库自 2026-09-10 起无新提交。
**角色**：**耿越 = 制作人 + 唯一有合并权的人**；**时浩然 = 实际学习者**（GitHub `shihao84an-design`，已是 collaborator，write 权限）。
**唯一未决事项**：[PR #2](https://github.com/yorhagengyue/study-os/pull/2) 自 2026-09-10 开着等审，见下方「当前阻塞点」。

## 系统定义

StudyOS 是耿越给时浩然做的**个人学习系统**，只服务一个人，不做多用户。两部分：

**Agent 端** —— Claude Code / Codex 直接在仓库里工作，读根 `CLAUDE.md` 的规则，帮时浩然理解课程、分析错题、扩展系统，并把过程写进 `docs/DEV-LOG.md`。`AGENTS.md` 只是指针，规则单一真源是根 `CLAUDE.md`。

**网页端** —— 本机 `python tools/app.py` 起服务，只听 `127.0.0.1:8765`。左边课程—模块—资料树，右边读正文和文件，顶部「刷新全部课程」；服务开着每 15 分钟自动刷一次。**网页端不含 AI 对话**，也不会唤起外部 Agent。

覆盖的资料范围 = 课件文件、课程页面、老师公告、作业说明。四门课：ACCT2011 / ACCT2019 / IBUS2020 / IBUS2105（悉尼大学 2026 S2）。

## 边界与红线（这个项目最重要的部分）

**身份边界**：学习者是时浩然，不是耿越。仓库里反复写明「不要把学习者与项目制作人混为一人」。record 仓 DRAWER.md 的候选事实 `c-9f1e3002` 正是这条——**study-os 的四门课不得当成耿越本人的学籍或课程**（must_confirm、open）。

**Canvas 只读**：只发 GET，不提交、不答题、不签到、不读成绩和提交记录。文档同时提醒：token 本身的权限由学校决定，**不能当成天然只读凭证**。

**凭证**：只放项目根被 git 忽略的 `.env`，不进代码、文档、日志、聊天、PR。`tools/save.py` 在提交前扫描 token 形态字符串，命中就拒绝提交。下载文件只进被忽略的 `state/`。

**`student/` 同步永不改写** —— 那是人写的学习记录（journal / stuck / ideas / ai-log / group 五类模板）。`raw/`、`content/` 是早期快照，不得冒充最新资料。

**合并权**：时浩然的改动一律走功能分支 + PR，**只有耿越审核通过才能合并**。技术检查通过、时浩然同意、Agent 自评都不能替代。Agent 不擅自合并、不直推 main。

**不代做考核**：可以拆题、举例、分析、辅导，不代写要交的作业或考试；课程要求用 Cogniti 的地方照课程做。

## 技术栈与代码地图

纯 Python 标准库 + 原生 HTML/JS，**零第三方依赖**（决策原文：降低上手门槛）。Python 3.9+。

| 文件 | 职责 |
|---|---|
| `tools/app.py` | 本机 HTTP 服务、资料浏览接口、自动刷新调度 |
| `tools/canvas.py` | Canvas 只读客户端：同源 GET、公网地址校验、逐跳重定向检查、下载跨域不带 token |
| `tools/material_sync.py` | 同步主力：翻页、模块/正文递归发现、下载、校验、原子替换、失败保留 |
| `tools/library.py` | 旧快照适配 + 正文净化（不执行教师页面脚本） |
| `tools/sync.py` | 旧命令入口 + 悉尼时区（含夏令时）换算 + 生成 `content/overview.md` 和 CHANGELOG |
| `tools/save.py` | 手动 Git 保存（带 token 扫描）；App 和同步**不**调用它 |
| `web/` | 三件套前端 |
| `docs/` | IDEA（耿越最初的分层构想）/ DECISIONS / DEV-LOG / VALIDATION / COURSES / CANVAS-API |

验证命令：`python -m unittest discover -s tests -v`（main 上 44 项，PR #2 分支 50 项）。

**一个做对了的设计**：学校的文件列表返回 403、页面列表返回 404 时，系统不把它记成「这门课没资料」，而是继续沿课程首页、模块条目和正文里的链接逐个取单项——实测靠这条路仍拿到 246 个文件。拿不到的如实标「拿不到」，保留原页入口和旧副本。

## 实跑证据（两轮，都标了「部分成功」）

**2026-09-09 · 耿越的 Windows 机器**（`docs/VALIDATION.md`）：四门课 28 模块 / 246 文件 / 142 页面 / 43 公告 / 42 份作业说明，下载 338.1 MB，246/246 文件长度与 SHA256 全对上，75 个 PDF 解析成功共 997 页，44 项测试过。

**2026-09-10 · 时浩然自己的 Mac**（`docs/DEV-LOG.md`）：本人配好 token 跑通，503 项资料 / 475 项本轮取得（247 文件、142 页面、44 公告、42 作业说明），338 MB，50 项测试过；浏览器实际点开当天新增公告和 Module 6 的 PDF 预览。

两轮共同的**未取得部分**：13 个文件被学校拒绝、2 个页面拿不到、2 份作业无正文、6 处列表接口权限限制（页面列表 404 / 文件列表 403）、11 个外部教学入口（LTI、视频、测验）不抓。另：**未做真实浏览器界面点击验收**（09-09 那轮），09-10 那轮补上了。

## 决策历史（`docs/DECISIONS.md` 提炼，原文更细）

- **2026-09-09 建仓** —— 私有仓放在耿越账号下，以后可转给时浩然；个人版先行；只用标准库；`raw`/`content` 系统写、`student` 人写、`state` 不上传；只拉本学期四门正式课（按 `ACCT2011` 这种课程代码形态筛）。
- **2026-09-09 优先级重排** —— 耿越明确：**最先解决「逐课逐文件下载 Canvas 资料」的负担**，再做基础资料树。顿悟/反思/过程记录推到下一阶段。这条**覆盖了 `docs/IDEA.md` 里「等展示层设计、先跑学习记录」的旧节奏**；IDEA 的其余边界仍有效。
- **2026-09-09 真实接入后的兼容修正** —— `hidden_for_user` 不再单独作为跳过附件的理由；Windows 长文件名改用同目录短名临时文件 + 原子替换（修好 4 个 PDF）。
- **2026-09-09 身份澄清 → 命名** —— 先确认「为朋友制作、朋友是学习者」，当天晚些耿越指定学习者姓名为**时浩然**；历史记录里的「朋友」即指他。
- **2026-09-09 规则集中化** —— 权威规则统一到根 `CLAUDE.md`，`AGENTS.md` 和 `docs/HOW-WE-WORK.md` 退化为指针。新增需求必须走「整理已有信息 → 只问关键缺口 → 写完整 Action Plan → 确认后实施 → 按结果验收」；错题必须按解题过程定位第一次出错的位置，不贴「粗心」标签。
- **2026-09-09 推送与合并条件** —— 时浩然的想法必须由他本人解释清楚、AI 核实记录准确后才可推送；耿越审核当前 PR 内容后才能合并。

## 当前阻塞点

**PR #2「修复 Mac 首次同步时附件连接长时间等待」自 2026-09-10 开着等耿越审核**（`codex/mac-canvas-connection`，2 个 commit）。

问题：时浩然第一次在 Mac 上同步时附件下载极慢。探测显示附件存储域名解析出 8 个地址、只有 1 个能在 3 秒内连上，而原实现对每个地址**串行等最多 45 秒**，已保存附件之间出现约 47.6 秒间隔。

改法：把单个地址的 TCP/TLS 建连预算压到 **5 秒**，握手成功后恢复原来的读取超时；优先重试同一主机上次成功的地址，但**每次仍重新校验 DNS**，旧地址只有仍在有效公共地址列表里才复用。只动 `tools/canvas.py`，加 6 条回归测试，50/50 通过；独立复核确认没有绕过公共 DNS 校验、证书校验或授权隔离。

## 与 Ripple Canvas 的关系

§8 [2026-08-24] 那条「USYD Canvas→iOS 推送项目」（定名 Ripple Canvas）跟本项目**是同一个学习者、同一个 Canvas、同一批四门课**。但 `~/Desktop/ripple-canvas-core` 和 `ripple-canvas-ios` **连 `git init` 都没做过**，最后修改是 2026-09-01，GitHub 上也没有这两个仓（2026-09-18 核实）。StudyOS（2026-09-09 起）实际上接替了那条线，形态从 iOS 推送换成本机网页 + Agent。**是否正式把 Ripple Canvas 标成被取代，owner 未拍板**。

## 已知的 Canvas 坑（`docs/CANVAS-API.md`，跨项目可复用）

- 文件列表 403、页面列表 404 = 权限问题，**不等于该课没资料**；按 id 单取往往能拿到。
- 四门课的 `syllabus_body` **全是空的**，真正的课程说明在页面、附件和学校官网大纲里。
- **0 分不等于可选**：ACCT2011 的 Draft Part A 门户 points 是 0，但指南写明必交。
- **作业标题不等于交付物**：IBUS2105 的 "Feasibility Report" 实际要交两份文件（Cogniti 生成的 PDF + 自己写的 2000 字反思 Word）。
- 权重看 `assignment_groups.group_weight`，不看单题 points（ACCT2011 有个权重 100 的 "Adjustment" 组，它不是考核）。
- 公告接口**默认只查近 14 天**，必须显式传 `start_date`。
- Canvas 时间是 UTC；悉尼 2026-10-04 进入 AEDT（+11）。
- 两门会计课的 tutorial 练的是**上一周** lecture 的内容。
- Canvas 和学校官网大纲有已知的不一致（ACCT2019 期中时长、IBUS2020 小组权重、ACCT2019 的 LO4/5）——系统的原则是**并列呈现、不替用户挑一个**。

## 下一步

1. **耿越审 PR #2**（唯一阻塞项，已等 8 天）。
2. 仓库所有权：DECISIONS 里写了「以后可以转到他自己的账号（GitHub Settings → Transfer）」，未执行。
3. IDEA 里 L5（时间与计划）、L7（展示层）属于产品设计，耿越没定之前不动。
